import 'package:flutter/material.dart';

class AppColors {
  // Primary color - 앱 전반적인 포인트 컬러
  static const Color primary = Color(0xFF4CAF50); // 녹색 계열, 강조 버튼 등

  // Secondary color - 보조 컬러
  static const Color secondary = Color(0xFF81C784); // 연녹색, 강조 요소

  // Background color - 화면 배경
  static const Color background = Color(0xFFF5F5F5); // 연한 회색, 기본 배경

  // Card color - 카드, 컨테이너 배경
  static const Color card = Color(0xFFFFFFFF); // 흰색, 카드 배경용

  // Text color - 기본 텍스트
  static const Color textPrimary = Color(0xFF212121); // 진한 회색, 기본 텍스트
  static const Color textSecondary = Color(0xFF757575); // 연한 회색, 보조 텍스트

  // Accent / Warning colors
  static const Color accent = Color(0xFFFFC107); // 주황색, 강조 아이콘/버튼
  static const Color error = Color(0xFFF44336);  // 빨간색, 오류 메시지
}